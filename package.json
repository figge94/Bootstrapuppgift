{
  "name": "bootstrap",
  "version": "1.0.0",
  "description": "bootstrap",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sass --watch --quiet-deps scss:css",
    "build": "sass scss:css"
  },
  "author": "Victoria Fingal",
  "license": "MIT",
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "bootstrap-icons": "^1.11.3"
  }
}
